package engine.registry.render.buff.image;

import engine.registry.render.buff.BufferedRenderConfig;

public class ImageRendererConfig extends BufferedRenderConfig {

    public ImageRendererConfig() {
        super();
    }
}
